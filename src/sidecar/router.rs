//! Sidecar router — stub, fully implemented in Task 2.

use crate::live_index::store::SharedIndex;

/// Build the axum Router with all 5 sidecar endpoints.
pub fn build_router(_index: SharedIndex) -> axum::Router {
    unimplemented!("build_router: implemented in Task 2")
}
